export = HttpServer;
declare class HttpServer {
    constructor({ port, backend }: {
        port: any;
        backend: any;
    });
    port: any;
    backend: any;
    app: any;
    start(): void;
    serverInstance: any;
    stop(): void;
    close(): void;
    rootHandler(req: any, res: any): Promise<void>;
    pingHandler(req: any, res: any): Promise<void>;
    relayHandler(req: any, res: any): Promise<void>;
}
